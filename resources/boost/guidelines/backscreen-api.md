---
name: backscreen-api
description: Guideline for the Laravel TMS / Backscreen API client package.
---

When working with the Backscreen/TMS API — adding or modifying endpoints, configuring clients, writing tests, or using the `TmsApi` facade — activate the **backscreen-api** skill.
